name: ICARUS Web Build
on:
  push:
    branches: [main, web-migration]
    paths: ['web/**','tests/native-contract.test.mjs','native/android/app/src/main/java/com/icarusalmighty/app/MainActivity.kt','.github/workflows/web-build.yml']
  pull_request:
    paths: ['web/**','tests/native-contract.test.mjs','native/android/app/src/main/java/com/icarusalmighty/app/MainActivity.kt','.github/workflows/web-build.yml']
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - run: npm test
      - run: node --test tests/native-contract.test.mjs
        working-directory: .
      - run: npm run build
