# Legacy Android bridge

This directory is the **deprecated ICARUS 1.2.0 bridge**.

Do not build, release, or add new features here. The canonical Android application is:

`native/android/`

Canonical package: `com.icarusalmighty.app`

Canonical CI/release workflow: `.github/workflows/android-build.yml`

This tree is retained temporarily for historical comparison while the remaining migration checks are completed. It may be removed after no release scripts, documentation, or tooling references it.
