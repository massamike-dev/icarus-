<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Private sideloaded tester only. The Play release intentionally does not
         request broad storage access; production file workflows use scoped APIs. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
</manifest>
